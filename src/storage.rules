rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow uploads, reads, and deletes only for authenticated users
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
